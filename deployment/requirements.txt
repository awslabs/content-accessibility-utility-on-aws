# Runtime dependencies for the AgentCore-hosted accessibility pipeline.
# Installed into the AgentCore Runtime container (Linux ARM64) at deploy time.
#
# We install the package from the LOCALLY BUILT wheel (bundled next to this
# file), not from PyPI, because the browser/agent layer is not yet published.
# The [agent] extra pulls in Strands + the AgentCore SDK so the hosted pipeline
# runs the render -> fix -> verify agent against the managed AgentCore Browser
# Tool. No local Chromium is installed — the browser is the managed service.
./content_accessibility_utility_on_aws-0.7.0-py3-none-any.whl[agent]
bedrock-agentcore
